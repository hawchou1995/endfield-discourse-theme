topic_list_width:
  default: "86%"
  type: string
  description: "话题列表及主内容区域的宽度。支持百分比(如 86%, 90%)或像素(如 1400px)。"

sidebar_stick_to_edge:
  default: true
  type: bool
  description: "【布局切换】左侧导航栏是否紧贴屏幕左边缘？(勾选: 紧贴屏幕边缘；不勾选: 紧贴中间的话题列表)"
