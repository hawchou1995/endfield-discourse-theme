topic_list_width:
  default: "86%"
  type: string
  description: "话题列表及主内容区域的宽度。支持百分比(如 86%, 90%)或像素(如 1400px)。"

sidebar_stick_to_edge:
  default: true
  type: bool
  description: "【布局切换】左侧导航栏和顶栏是否紧贴屏幕边缘？(勾选: 绝对贴边；不勾选: 居中收缩)"
